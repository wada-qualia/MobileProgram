<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev builds talk to a local HTTP backend inside the LAN. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
